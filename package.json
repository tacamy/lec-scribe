{
  "name": "lec-scribe",
  "version": "0.1.0",
  "private": true,
  "description": "A general-purpose Chrome extension for taking notes while you watch a video: local audio recording, slide-change screenshots, and on-device Japanese transcription with WhisperKit. For personal study; follow the terms of the site you watch.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "oxlint extension/src extension/entrypoints server/src server/scripts scripts fixtures",
    "fixtures:make": "node fixtures/make-slides.mjs",
    "fixtures:serve": "node fixtures/serve.mjs"
  },
  "devDependencies": {
    "oxlint": "^1.82.0",
    "playwright": "1.56.1"
  }
}
