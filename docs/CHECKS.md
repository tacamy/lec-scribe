# 動作確認手順

tabCapture を伴う確認は Mac 実機でしかできないため、Phase ごとの手動確認手順をここにまとめる。結果は本ファイル末尾の「記録」に追記する。

## 共通: 拡張の読み込み

```sh
pnpm install
pnpm build            # extension/dist/chrome-mv3 が生成される
```

1. Chrome で `chrome://extensions` を開き、右上の「デベロッパーモード」を ON
2. 「パッケージ化されていない拡張機能を読み込む」→ `extension/dist/chrome-mv3` を選択
3. ツールバーの拡張アイコンから LecScribe をピン留め
4. コードを変更したら `pnpm build` → `chrome://extensions` の更新ボタン（↻）

UI の流れ: 動画のタブでツールバーのアイコンをクリックするとポップアップが開く。このクリックがタブへのキャプチャ許可（activeTab）を兼ねるので、`Start` は必ずポップアップから押す。`Start` を押すと右側にサイドパネルが開き、ポップアップは閉じる。パネルはページを操作しても閉じないので、状態の確認と `Stop`、エクスポートはパネルで行う。パネルの `Start` は許可が切れていると失敗する（メッセージが出る）。

エラーは `chrome://extensions` の LecScribe カード内「エラー」と、service worker の DevTools（カード内の「Service Worker」リンク）、パネルの DevTools（パネル内を右クリック →「検証」）で確認する。

## Phase 1: tabCapture + AirPods パススルー

目的: スライド動画を再生中に Start しても、AirPods から音が途切れず、二重にも聞こえないこと（SPEC D-02、Q-12）。

準備: AirPods 等を接続し、macOS の出力デバイスに選ぶ。動画のあるページを開き、動画を再生する。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画のタブでツールバーの LecScribe アイコンをクリック | ポップアップに `● Ready` と `Start` ボタンが表示される |
| 2 | `Start` | ポップアップが閉じ、右側にサイドパネルが開く。状態が `● Capturing`、経過時間が進む。`Audio` 行が「録音中」になり、レベルメーターが音声に合わせて動く（「無音」表示にならない） |
| 3 | 聞く | AirPods から動画の音声がそのまま聞こえる。エコー / リバーブのような二重再生になっていない。音量が変わっていない |
| 4 | タブを見る | タブに「共有中」のインジケータ（録画アイコン）が出る |
| 5 | パネルを閉じ、アイコンをクリックしてポップアップを開く | `● Capturing` のまま、経過時間が継続している |
| 6 | 動画を一時停止 → 再開 | 約 1 秒後に「無音」と表示されメーターが 0 になる。再開で動く。経過時間は止まらない（録音を止めない仕様、SPEC §7.3） |
| 7 | `Stop` | 状態が `● Ready` に戻り、フッターに「前回: HH:MM:SS キャプチャ（user）」。動画の音は引き続き AirPods から聞こえる |
| 8 | もう一度 `Start` → `Stop` | 2 回目も同じ挙動 |

異常系:

| # | 操作 | 期待 |
|---|---|---|
| A | `chrome://extensions` など Chrome 内部ページのポップアップで `Start` | 赤いメッセージ「このページはキャプチャできません…」、状態 `● Error`。`Start` で再試行できる |
| B | キャプチャ中に動画のタブを閉じる | パネルが `● Ready` に戻り、前回の理由が `tab closed` |
| C | キャプチャ中に `chrome://extensions` で拡張を更新（↻） | 開き直すと `● Done`、前回の理由が `extension restarted`。セッションは「中断」として残る |
| D | サイドパネルの `Start`（アイコンを押さずに） | 「タブのキャプチャを開始できません…アイコンをクリックして…」のメッセージ。ポップアップからの `Start` で開始できる |

二重に聞こえる場合（パススルー不要な環境）:

パネル内を右クリック →「検証」→ Console で次を実行してから `Start` し直す。

```js
chrome.storage.local.set({ config: { audio: { passthrough: false } } });
```

戻すときは `passthrough: true`。この結果は SPEC D-02 の既定値に反映する。

## Phase 2: 録音（OPFS へ逐次保存 → エクスポート）

目的: 動画の音声が `audio.webm` として残ること。途中で拡張が再起動しても直前のチャンクまで残ること（SPEC D-06）。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画ページで `Start` → 2〜3 分待つ | パネルの Audio 行が「録音中 1.2 MB」のように増える（64 kbps なので 1 分あたり約 480 KB） |
| 2 | `Stop` | 状態が `● Done`、Audio 行が「録音完了 00:03:00 / 1.4 MB」。下に「保存済みセッション」が出る |
| 3 | セッションの「エクスポート」 | 状態が `Exporting…` になり数秒で戻る。`~/Downloads/LecScribe/<日時>/` に `audio.webm`、`session.json`、`status.json` ができる |
| 4 | `audio.webm` を Chrome の新しいタブにドラッグ＆ドロップして再生 | 動画の音声が聞こえる。長さが録音時間と一致する（`ffprobe audio.webm` でも確認できる） |
| 5 | もう一度 `Start` → 30 秒待つ → `chrome://extensions` で拡張を更新（↻） | 開き直すとリストに「中断」タグ付きのセッションがあり、エクスポートすると 20〜30 秒分の `audio.webm` が得られる |
| 6 | セッションの「削除」 | パネル内の確認ダイアログのあとリストから消える。拡張内の録音と `~/LecScribe/` のフォルダの両方が消える（2026-09-11。それまでは「破棄」で拡張内だけを消していた） |

`session.json` には開始時刻（`startedAt`）と設定、`status.json` には終了時刻・バイト数・録音時間が入る。これらは Phase 8 の時刻合わせに使う。

## Phase 3: 動画要素の検出

目的: 動画ページの `<video>`（Brightcove / video.js）が見つかり、録音中にその状態を追えること（SPEC §8.1, §8.2）。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画ページで動画を再生した状態でアイコンをクリック | ポップアップの Video 行に「video.js 1280×720 · 再生中」のように出る（Start 前の probe） |
| 2 | `Start` | サイドパネルの Video 行に「video.js 1280×720 · 1x · 再生中 · 00:12:34」。10 秒以上見て、時刻が進み続ける（報告が届かなくてもパネルは 6 秒までは進めて見せるので、それより長く見る。2026-09-17） |
| 3 | 動画を一時停止 / 再開 / シーク | Video 行が数秒以内に追従する（一時停止で「一時停止」、シークで時刻が飛ぶ） |
| 4 | 再生速度を 1.5x にする | 警告「再生速度が 1.0x ではありません…」が出る。1.0x に戻すと消える |
| 5 | 別のタブに切り替える | 警告は出ず、Video 行の時刻が 10 秒以上進み続ける（報告が止まっても 6 秒までは進んで見えるため、それより長く見る。Chrome はキャプチャ中のタブを表示扱いのままにして描画も続けるため。Phase 4 で背景中もフレームが撮れるか確認する）。Chrome のウィンドウを最小化した場合の挙動も記録する |
| 6 | 動画のタブ内で別のページへ移動する | 警告「動画ページから移動しました…」。録音は続く（Audio の MB が増える） |
| 7 | 動画のないページ（例: ニュースサイト）で `Start` | Video 行「動画なし（音声のみ）」と警告「このページには動画が見つかりません…」。録音は動く |
| 8 | `Stop` | 通常どおり `● Done` |

任意: 動画ページの DevTools Console で `document.querySelector('video').mediaKeys` が `null` なら DRM なし（警告 DRM は出ない）。`taintFree` の判定結果はサイドパネルでは見えないが、警告「この動画からはスライド画像を取得できません」が出なければ canvas に描ける（Phase 4 の前提）。

## Phase 4: フレーム取得

目的: `<video>` を canvas に描いた画像が動画のネイティブ解像度で保存され、プレイヤー UI やカーソルが映らないこと（SPEC D-03, §8.3）。変化検知は Phase 5 なので、ここでは開始時の 1 枚とパネルの「スクショを保存」で確認する。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画ページで `Start` | サイドパネルの Slides 行が「1 枚（最終 00:12:34）」になる（開始時の自動保存） |
| 2 | スライドが切り替わったところで「スクショを保存」 | 緑のメッセージ「slide_002.png を保存しました（1280×720、…）」。Slides 行が 2 枚になる |
| 3 | 別のタブを表示したまま、サイドパネルの「スクショを保存」 | 保存できる。あとで見たとき、その時点の映像が写っている（真っ黒や古いフレームでない） |
| 4 | 動画を全画面にして戻す、ウィンドウを小さくする | どちらの状態でも「スクショを保存」が同じ解像度で保存される（表示サイズに依存しない） |
| 5 | `Stop` → エクスポート | `~/Downloads/LecScribe/<日時>/slides/slide_001.png …` と `slides.json` ができる |
| 6 | 画像を開く | 動画の映像だけが写り、再生バー・カーソル・ページの周辺は映っていない。解像度が `1280×720` などネイティブ値 |
| 7 | `slides.json` を開く | 各要素に `videoTime`（動画内の秒）、`t`（録音開始からの秒）、`reason`（initial / manual）が入っている |

うまくいかない場合: 「この動画は canvas に描けないため…」が出たら tainted（Phase 4 の前提が崩れるので報告してほしい）。「動画がまだ読み込まれていません」は再生を始めてから押す。

## Phase 5: 変化検知

目的: スライドが切り替わったときだけ自動保存され、講師のワイプや小さな動きでは保存されないこと（SPEC §9）。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画を 10〜15 分そのまま視聴する | Slides 行の枚数が、スライドの切り替わりに合わせて増える。切り替えから 1〜2 秒後に増える |
| 2 | Video 行の「変化 x.x%」を見る | 何も変わっていないとき 1% 未満。スライドが切り替わった直後は数% 以上 |
| 3 | 講師だけが動いている場面 | 枚数が増えない |
| 4 | 同じレイアウトで文字だけ変わるスライド | 増える（見逃したらその時刻を記録して報告。閾値調整の材料） |
| 4b | 保存されるたびに動画の右下にサムネイルだけのトーストが約 2.5 秒出る。全画面でも出る | 手動保存も同じ表示 |
| 4i | メニューを開いただけ・少しスクロールしただけの画面や、同じ写真の続きが notes.md に並ばない | `scenes.json` に `reason: vision` で残る。補助コマンドはサーバーが起動時に作る（server.log に「Vision の補助コマンドをビルドします」「作りました」が出る） |
| 4j | 同じ字幕の付いた映像の続き（被写体がゆっくり動く）や、講師が手を動かしただけの場面が notes.md に並ばない | `scenes.json` に `reason: text`（文字が同じ）で残る。字幕が薄く出かけの画像は `reason: grown`。場面が少しずつ変わる並びでは `via` に直前の画像 |
| 4k | 同じテンプレートで文字だけ違うスライドが続く | まとめられずに全部載る（文字が違うため） |
| 4h | まったく同じ画像が 2 枚並ばない | `notes.md` に同じ画像が続かない。`scenes.json` に `reason: identical` として残る |
| 4g | 映像中心の動画を文字起こしする | `notes.md` には同じ場面の画像が 1 枚だけ載る。`.lecscribe/scenes.json` に外した画像と理由（`sameSceneAs` / `colorMatch`）がある。`slides/` には全部残っている |
| 4f | 被写体を映している場面（スライドではない映像）が続く | 被写体が動いただけでは撮られない。場面が変わったときだけ増える |
| 4e | 講師のワイプ（顔出しの小窓）で先生が動く。スライドは変えない | キャプチャされない（トーストが出ない）。数分見ていて増えないこと |
| 4d | スライドの本文が少しだけ変わる（1 行増える、数語が変わる） | 新しいスライドとしては保存されず、次のスライドに移った瞬間に画像が最後の状態で上書きされる（`slides.json` の `updated: true`）。講師のワイプが動いただけでは上書きされない |
| 4c | 文字が 1 行ずつ出るスライド | 切り替わり直後に保存されたあと、次のスライドに移った瞬間にもう一度トーストが出て、`slides/slide_N.png` が全部出た状態になる。`slides.json` の `updated: true` |
| 5 | 一時停止 → 再開、シーク | 同じ画面に戻った場合は増えない。別のスライドに飛んだ場合は増える |
| 6 | `Stop` → エクスポート → `slides/` を見る | 枚数がスライド数とほぼ一致し、同じスライドの重複がない（あれば何枚か報告） |

報告してほしいこと: 動画のスライド数（目視）と保存枚数、見逃し・重複があればその時刻。`slides.json` の `reason` が `change` のものが自動保存分。

## Phase 6: タイムライン記録

目的: 一時停止・シーク・速度変更を含む視聴の履歴が `timeline.json` に残り、録音時刻から動画時刻を復元できること（SPEC §10）。

| # | 操作 | 期待 |
|---|---|---|
| 1 | `Start` → 数分視聴する間に、一時停止 → 再開、10 秒戻す、再生速度を 1.5x にして 1.0x に戻す | パネルの表示は変わらない（速度変更中は警告が出る） |
| 2 | `Stop` → エクスポート | `timeline.json` ができる |
| 3 | `timeline.json` を開く | 先頭が `start`、末尾が `stop`。操作した順に `pause` / `play` / `seeked` / `ratechange` が並び、`videoTime` が操作時の動画の時刻、`t` が録音開始からの秒。再生中は 10 秒ごとに `tick` |
| 4 | `slides.json` と見比べる | 各スライドの `t` から timeline で動画時刻を求めると `videoTime` と一致する（例: 一時停止中に手動保存したスライドの `videoTime` は停止位置のまま） |

## Phase 7: ローカルサーバーと WhisperKit

目的: Stop 後に録音がサーバーへ送られ、`~/LecScribe/<タイトル>_<日時-ID>/` に日本語の文字起こし（SRT / VTT / TXT / JSON）ができること（SPEC §12, §13）。

準備:

```sh
brew install whisperkit-cli ffmpeg
pnpm --filter @lec-scribe/server start
```

初回は `large-v3` モデル（数 GB）のダウンロードに時間がかかる。サーバーはターミナルを開いたままにするか、`pnpm --filter @lec-scribe/server agent:install` で常駐させる（README「サーバーの常駐化」）。常駐後にサーバーのコードを更新したら `agent:restart`。

| # | 操作 | 期待 |
|---|---|---|
| 1 | ポップアップに出る「このMacと接続」（設定画面にもある）→ Mac に出るダイアログで「許可」→ 設定画面の接続テスト | ダイアログに拡張名と ID が出る。「接続しました」と出て、接続テストは「承認: 済み / whisperkit-cli: あり / ffmpeg: あり」。`~/.lec-scribe/trusted.json` に拡張 ID とトークンが書かれる |
| 2 | 動画を 2〜3 分録音して `Stop` | 状態が `Uploading` → `Transcribing` と進み（末尾の点が 1 文字ずつ増える）、Server 行に「送信中 xx%」→「文字起こし中 · 経過時間」 |
| 3 | 待つ | 状態が `● Done`、Server 行が「完了 · /Users/…/LecScribe/…」。サーバーのターミナルにも進捗が出る |
| 4 | 出力フォルダを開く | 見えるのは `notes.md` と `slides/` だけ。Finder で Cmd+Shift+. を押すと `.lecscribe/` が見え、中に `audio.webm`、`transcript.json` / `.srt` / `.vtt` / `.txt`、`lecture.md`、`slides.json`、`timeline.json`、`session.json`、`pipeline.json` |
| 5 | `.lecscribe/transcript.srt` を開く | 日本語の文字起こしが動画時刻で並んでいる（一時停止・シークがあっても動画の位置に合う） |
| 6 | サーバーを止めた状態で録音 → `Stop` | 警告「ローカルサーバーに接続できません」。セッションは残り、サーバー起動後に一覧の「送信」で文字起こしできる |
| 7 | 拡張を更新（↻）した直後に「送信」 | 文字起こしが動く（再送でサーバー側は上書き） |
| 8 | 文字起こし中に次の動画で `Start` → 数分録音して `Stop` | 録音は普通にできる（Server 行に前の処理の段階が出続ける）。Stop 後は Server 行に「送信待ち 1 件」。前の処理が終わると自動で送られ、順に文字起こしされる |
| 8b | 文字起こし中に一覧の別のセッションで「やり直す」 | ボタンは押せて、その行に「送信待ち」のタグ、Server 行に「送信待ち N 件」。他の行の「やり直す」も押せたまま。前の処理が終わると順に送られる |
| 9 | 文字起こし中に一覧のそのセッションの「中止」（初回の処理） | パネル内の確認ダイアログのあと処理が止まり（サーバーのログに cancelled）、`~/LecScribe/` のフォルダと拡張内の録音が消える。送信待ちがあれば次が始まる |

処理時間の目安と精度（`large-v3` と `large-v3_turbo` の比較）は結果欄に記録する。

## Phase 8: 統合（lecture.md）

目的: スライドごとに画像とその間の発話が並んだノートができること（SPEC §13.4, D-14）。

| # | 操作 | 期待 |
|---|---|---|
| 1 | 動画を 10 分ほど録音して `Stop`、文字起こし完了を待つ | 出力フォルダに `notes.md` ができる（`--llm` なしなら文字起こしそのまま。同じ内容の `lecture.md` が `.lecscribe/` にもある） |
| 2 | `notes.md` を Markdown ビューア（VS Code のプレビューなど）で開く | 節見出しも区切り線もなく、スライド画像 → そのスライドの間に話された内容（句点ごとに改行）が順に並ぶ。画像は文の途中には挟まらない |
| 3 | 画像と本文を見比べる | 本文がそのスライドの話になっている。切り替え直後の発話が前のスライドに混ざる場合はその時刻を報告（自動検知分は 1.5 秒早く始まったとみなして割り当てている） |
| 4 | `.lecscribe/transcript.json` を開く | 各区間に `slide` が入っている（スライド前の区間にはない） |
| 8b | 文字起こしが文の途中で区間に分かれている箇所でスライドが変わる | notes.md / lecture.md で画像が文の途中に挟まらず、文の切れ目に入る。`transcript.json` で同じ文の区間が同じ `slide` になっている |

## Phase 9: ノート作成（notes.md）

目的: 話し言葉の文字起こしを書き言葉に整え、節ごとの要点が付いた `notes.md` ができること（SPEC §13.5）。

準備（`codex exec` を使う場合）:

```sh
brew install --cask codex
codex login
codex exec "こんにちは、と返答して"     # 返答が来れば OK
```

常駐サーバーに設定を渡す（環境変数は登録時に plist へ書き込まれる）:

```sh
LEC_SCRIBE_LLM=codex pnpm --filter @lec-scribe/server agent:install
```

| # | 操作 | 期待 |
|---|---|---|
| 0 | 拡張の設定画面を開く | 「ノート作成」に今の状態が出る。無効なら `bash ~/LecScribe-app/enable-notes.sh` がコピーボタン付きで出る |
| 1b | 続けてもう一度「やり直す」 | LLM を呼ばずに終わる（ログに「前回のノートを使い回します」、`pipeline.json` に `notesReused: true`）。数秒で完了する |
| 1 | 文字起こし済みのセッションで「やり直す」 | 文字起こしは飛ばされ（ログに「transcript を再利用」）、Server 行が「ノート作成中」を経て数分で完了する（ログに `batch n/m` のあと `outline` が 1 回出る） |
| 2 | 出力フォルダを開く | `notes.md` が整えた版に置き換わっている。文字起こしそのままの `lecture.md` は `.lecscribe/` に残っている |
| 3 | `notes.md` を開く | 冒頭に「全体の要点」。本文は話題ごとの見出しと「要点」で区切られ、その中にスライド画像とフィラーの取れた本文が並ぶ。見出しの区切りが内容に合っている（画面が細かく変わる操作デモでも話題単位になっている）。内容が変わっていない（要約・補足されていない） |
| 4 | `pipeline.json` を見る | `result.notes: true`。`notesError` があれば枠切れなどでその節が文字起こしのまま |

## 残っている実機確認（2026-09-09 時点）

smoke テストでは通っているが、Mac 実機ではまだ確認・記録していないもの。確認したら「記録」に追記する。

| # | 確認すること | 手順 | 必要な準備 |
|---|---|---|---|
| ~~N~~ | 未接続のときポップアップは説明と「このMacと接続」だけになる（Start は出ない）。押すと Mac のダイアログが出て（ポップアップは閉じてよい）、「許可」で接続済みになり通常の画面に戻る。「許可しない」だと未接続のまま。承認後は録音 → Stop で送信できる | Phase 7 の手順 1 | `agent:restart` 済み、拡張 ↻ |
| K | サイドパネルが録音を始めたタブにだけ出る。別のタブに移るとパネルが消え、戻ると再び出る。別のタブでアイコンを押すとポップアップに録音中の行（Audio / Video / Slides / Tab）が出る | ポップアップで Start → 別タブへ → 戻る | 拡張 ↻ |
| L | 動画のタブで次の動画に移動（ページ遷移）したあともパネルが残るか。消える場合は次の Start で開き直せばよい | 録音を Stop したあと同じタブで別のページへ | — |
| M | 待機中のポップアップ / サイドパネルに Audio / Video / Slides / Tab の行が出ず、Server 行と Start と一覧だけ。録音完了直後は Audio 行に長さとサイズが残る | 待機中と Stop 直後に見る | 拡張 ↻ |
| ~~A~~ | 文字起こし中の「破棄」で処理が止まり、`~/LecScribe/` のフォルダと拡張内の録音が消える | Phase 9 の手順 9 | `agent:restart` 済み、拡張 ↻ |
| ~~B~~ | 破棄のあと `ps aux \| grep -E 'whisperkit\|codex'` に子プロセスが残らない（codex は Node ラッパー経由なので本体が残る可能性あり） | 手順 A の直後に確認 | 同上 |
| C | `notes.md` が「全体の要点」→ 話題ごとの見出し・要点 → 画像と本文、の形になっている（スライドごとの要点はない） | 処理済みのセッションで「やり直す」→ フォルダを開く（Phase 9 の手順 3） | `agent:restart` 済み |
| D | 文字起こし中に次の動画で Start → Stop すると「送信待ち 1 件」になり、前の処理が終わると自動で送られる | Phase 9 の手順 8 | 長めの動画 2 本 |
| ~~E~~ | ポップアップで Start を押すだけで録音が始まり、サイドパネルが開く（Start を 2 回押さなくてよい） | Phase 1 の手順 1〜2 | 拡張 ↻ |
| ~~F~~ | 録音中もサイドパネルに保存済みセッションの一覧が出て、録音中でないセッションの「破棄」「フォルダを開く」が押せる（「Downloads に書き出す」はその後 UI から外した） | 録音中にパネルを見る | — |
| G | 一覧の処理中・送信待ちの行に「処理中」「送信待ち」のタグが出る | 手順 D の途中でパネルを見る | — |
| ~~H~~ | 常駐サーバーに `--llm codex` が渡っている（`agent:status` のコマンドラインか、処理後の `pipeline.json` の `result.notes: true`） | `pnpm --filter @lec-scribe/server agent:status` | `LEC_SCRIBE_LLM=codex … agent:install` していなければやり直す |
| I | 90 分の動画を通しで処理できる（SPEC の見込みは Done まで 25〜30 分、Codex 込みでさらに数分） | 実際の動画 1 本 | 時間 |
| J | Chrome のウィンドウを最小化しても録音・スライド保存が続くか（Phase 3 の手順 5 で未記録） | 録音中に最小化して数分待つ | — |
| O | サーバーが動いていないとき、ポップアップに install の 1 行とコピーボタンが出て、「このMacと接続」は出ない。`agent:uninstall` → アイコン → 表示を確認 → `install.sh` を実行（別 Mac か、`LEC_SCRIBE_APP_DIR` を変えて）→ サーバーが見つかり接続できる | `pnpm --filter @lec-scribe/server agent:uninstall` | 元に戻すには `agent:install` |
| P | 同じ字幕の映像の続きや講師の手の動きだけの場面が 1 枚にまとまる（7 章の 006〜012 と 016〜017、8 章の 004〜005 と 012〜013）。文字だけ違うスライドや、ラベルの違う装置の写真（6 章の 007 と 010）はまとまらない | 7 章・8 章で「やり直す」→ `scenes.json` の `reason: text` / `grown` / `via` | `agent:restart` 済み（補助コマンドは起動時に作られる） |
| Q | 画像が文の途中に挟まらない（7 章「メスが直接幼虫を産むというのが / 画像 / イメージしにくい…」） | 7 章で「やり直す」→ notes.md | 節の区切りが変わった節だけ LLM を呼び直す |
| R | 処理中の行の「中止」: 初回なら `~/LecScribe` の途中のフォルダも Chrome 側の録音も消えて行が消える。やり直し中なら処理だけ止まり、行は処理済みに戻って notes.md も残る | 長めの動画で Stop 直後に「中止」/ 処理済みの行で「やり直す」→「中止」 | — |
| S | 処理済みの行の「非表示」で行が消え、一覧の下の「非表示のセッションを表示（N）」で戻せる。「表示」で元に戻る | 処理済みの行で「非表示」 | データは消えない |
| T | 処理済みの行の「削除」で確認が出て、OK で `~/LecScribe` のフォルダ（notes.md があっても）と行が消える | 要らない録音（3 章の撮り直し分など）で「削除」 | 元に戻せない |
| U | Finder で `~/LecScribe` のフォルダを消した行に「データなし」が付き、「フォルダを開く」「やり直す」が押せない。「削除」で行を片付けられる | フォルダを消してからパネルを開き直す | サーバー起動中 |
| V | 全画面再生（フレーム全体が動く）の動画で、場面が変わったときにキャプチャが増える | 全画面表示や、画面いっぱいの映像が続く動画で録音 | 2026-09-11 の修正。直前まで 1 枚も増えなかった |
| W | Stop したとき、最後のスライドが最終状態（文字が全部出た状態）で保存される | 文字が 1 行ずつ出るスライドを最後に映したまま Stop | 2026-09-11 の修正。直前まで動いていなかった |
| X | ノート作成に失敗したとき、`notes.md` が文字起こしそのままの本文になる。「中止」なら前回の `notes.md` が残る | Codex の枠が切れた状態で「やり直す」/ やり直し中に「中止」 | — |
| Y | `--llm-model` を変えて「やり直す」と、前のモデルの文章が使い回されず作り直される | `enable-notes.sh` でモデルを変えてから「やり直す」 | トークンを使う |
| Z | 旧 `<日時-ID>_<タイトル>` のフォルダのセッションでも「やり直す」「フォルダを開く」が動く | 9 月 10 日以前に作ったセッション | 後方互換 |
| AA | `imagefp.swift` が変わるコミットを取り込んだあと、起動直後の server.log に「Vision の補助コマンドをビルドします」と「作りました」が数秒で出る。そのあとの文字起こしでビルド待ちが起きない（`update.sh` でも自動更新でも同じ） | `imagefp.swift` にコメントを 1 行足して `agent:restart` → ログを見る → 1 本処理する | **終わったら `git checkout server/tools/imagefp.swift` で戻す**（手元に変更があると AB の自動更新が動かない）。#10 |
| AB | サーバーの自動更新: main が進んだ状態で `agent:restart` すると、ログに「新しい版があります」→「入れ替えました」と出て、新しいコードで起動し直す（`git log` が進んでいる）。更新が無いときは「最新です」、ブランチ作業中は「ブランチが main ではありません」 | main を進めてから `pnpm --filter @lec-scribe/server agent:restart`、`~/Library/Logs/lec-scribe/server.log` を見る | `agent:install` を 1 度通して plist に `LEC_SCRIBE_MANAGED` を入れてから |
| AC | ネットワークを切って `agent:restart` しても、サーバーがクラッシュループにならず普通に起動する（ログに「自動更新: …見送る」旨が出て `/health` が返る） | Wi-Fi を切って `agent:restart` → `curl 127.0.0.1:47321/health` | 20 秒ほどで起動する |
| AD | サーバーを止めた状態で 2 本 Stop すると、Server 行に「送信待ち 2 件」と警告が出て行列が消えない。サーバーを起動すると、間隔を空けながら自動で送られて消える（すぐ試すなら一覧の「文字起こしする」） | `agent:uninstall` → 2 本録って Stop → `agent:install` | 元に戻すには `agent:install`。#8 |
| AE | 古いサーバー（`api` を返さない 2026-09-11 より前の版）に繋ぐと、パネルの警告に「Mac 側のサーバーが古く…」が出て、設定画面の「接続テスト」にも同じ文が出る。`update.sh` で消える | 開発機で古いコミットを checkout して `pnpm start`、拡張を開く | #7。手元では main を古いコミットに戻して試す |
| AF | `agent:install` のあと、`agent.mjs print-launcher` の出力どおりの起動用アプリができていて、サーバーが普通に起動する（`agent:status` で `/health` が返る）。`start.sh` にログを 1 行足して `agent:restart` すると、それが server.log に出る（起動コマンドを git 側で変えられる証拠）。`start.sh` を一時的に別名にして `agent:restart` しても、直接起動に落ちて起動する（クラッシュループにならない） | `agent:install` → `agent:status` → `start.sh` を編集 → `agent:restart` → `start.sh` を退避 → `agent:restart` | **終わったら `git checkout server/scripts/start.sh` と退避の戻し**。#10 |
| AG | 設定画面の「接続テスト」と `agent:status` に「見た目の判定（Vision）: あり」と出る。`mv ~/.lec-scribe/bin ~/.lec-scribe/bin.bak` してもう一度押すと「まだ作っていません。次の文字起こしのときに作ります」になり、`xcode-select` は勧めない。そのまま 1 本文字起こしすると作り直され、「あり」に戻る（server.log に「ビルドします」「作りました」）。`agent:restart` の直後に押すと「準備中」が出ることがある | 「接続テスト」→ `mv` → 押し直す → 1 本処理 → 押し直す | 終わったら `bin.bak` を消す。#17 |
| AH | 録音中、Video 行の再生位置が 1 秒刻みで進み、5 秒ごとに飛ばない。一時停止すると止まり、再開すると続きから進む。1.5x にすると 1.5 倍の速さで進む。終わりまで再生しても動画の長さを超えない。行の幅が毎秒揺れない（パネルの幅を変えても、行が 1 行と 2 行を行き来しない）。パネルを開いたまま Start したとき、Video 行に「動画なし（音声のみ）」が一瞬出ない | 動画を再生して Start → パネルを見る → 一時停止・再開・速度変更 → 終わりの近くへシークして最後まで再生 → Stop してパネルを開いたまま Start | 「変化 x%」は 5 秒ごとのままでよい |
| AI | 前の講義を文字起こししている間に、次の講義のタブでポップアップを開くと、Video 行に「video.js 1280×720 · 再生中」のような Start 前の確認が出て、処理の段階が進んでも（Server 行が変わっても）消えない。動画の無いページなら「動画が見つかりません」の警告が残る | 1 本目を Stop → 2 本目のタブで動画を再生 → ポップアップを開いて数十秒待つ | 2026-09-17 の修正まではここで消えていた |
| AJ | 6 章（グラフィックデザイン）を「やり直す」と、講師の動きだけが違う 073〜075 が 1 枚（075）にまとまり、076（板書なし）は残る。15 章（グラフィックデザイン）では Illustrator のメニューを開閉した 067〜068 が 1 枚になる。ほかの章は変わらない（26 セッションで再判定して確認済み） | 6 章と 15 章で「やり直す」→ `scenes.json` の `via` と `shown` を見る | Codex は呼ばれない（節がつながるだけなので前回の結果を組み替える） |
| AK | 接続の承認（トークンは承認ごとに 1 つ）。①設定画面で「このMacと接続」を押し直してもダイアログは出ず、接続済みのまま（文字起こし中に押しても一覧はエラーにならない）。②`curl -s -X POST -H 'Origin: chrome-extension://<拡張 ID>' http://127.0.0.1:47321/pair` ではトークンが返らず、ダイアログに「この ID の拡張はすでに接続済みです」が出る。Return で押される既定のボタンは「許可しない」で、押すと 403。③ID を `bbbb…`（b を 32 個）にして同じ curl を送ると「すでに別の拡張が接続済みです: LecScribe（ID: …）」が出る。④「接続を解除」→ 確認で OK → 未接続になり、`~/.lec-scribe/trusted.json` からこの拡張の行が消える。「このMacと接続」→「許可」で戻る。⑤起動時の server.log にトークンが出ない。新しい講義のフォルダは `ls -ld` で `drwx------` | `agent:restart` → ①〜④ → 1 本録音 | 2026-09-18 のセキュリティレビュー。既にある `~/LecScribe` の権限は変わらない |
| AL | ①設定画面に「詳細設定（ポート / トークン）」が無く、「このMacと接続」「接続を解除」「接続テスト」だけが並ぶ。接続テストの結果はボタンの文字と 1 行目がそろう。②ほかのアプリが 47321 を使っているとき、`agent:install` が「ポート 47321 を「Python」が使っているため、LecScribe のサーバーを起動できません。…」と出して終わる。設定画面の「接続テスト」とパネルにも「ポート 47321 をほかのアプリが使っているため…」が出る。③そのアプリを止めると、10 秒ほどでサーバーが自動で起動し、`agent:status` が応答ありになる | `agent:uninstall` → `python3 -m http.server 47321 --bind 127.0.0.1` → 別のターミナルで `agent:install` → 設定画面とパネルを見る → python を Ctrl+C → `agent:status` | 2026-09-18。§12.1d |
| AM | 11 章（グラフィックデザイン I-3）を「やり直す」と、Illustrator のキャンバスを少しずらしただけの 122 と 123 が 1 枚（123）にまとまる。10 章（同）の 028→029、グラフィックデザイン I-2 の Web ページを少しスクロールした 054→055・056→057 も 1 枚ずつになる。ほかの章は変わらない（27 セッション 1,044 枚で再判定して確認済み） | 11 章で「やり直す」→ `scenes.json` で 122 が `superseded`、123 に `standsFor` が付く | 2026-09-20。Codex は呼ばれない（節がつながるだけなので前回の結果を組み替える）。§13.4b |
| AN | グラフィックデザイン I-2 の 1 章を「やり直す」と、同じ 2 画面を行き来した 023〜026 が 2 枚（023 と 026）になる。`scenes.json` で 025 が `revisit`（`via` は 023）、024 が `superseded`。戻っていた 7 秒の間の話は 026 の画像の下に入る。戻って 10 秒以上話している箇所（同じ章の 008 = 60 秒）は画像が残る | 1 章で「やり直す」→ `notes.md` と `scenes.json` を見る | 2026-09-20。§13.4b。Codex は呼ばれない（節がつながるだけなので前回の結果を組み替える） |
| AO | ノートを整えられなかった講義が一覧で分かる。Codex が失敗する状態（利用上限に当たった、または `LEC_SCRIBE_CODEX=/usr/bin/false pnpm --filter @lec-scribe/server agent:install` で一時的に失敗させる）で 1 本「やり直す」→ 状態は完了のまま、その行に「ノートを整えられませんでした。時間をおいて「やり直す」を押してください」が出て、乗せると理由が出る。`notes.md` は文字起こしのまま。元に戻して（`LEC_SCRIBE_CODEX= … agent:install`）もう一度「やり直す」と、注意書きが消えてノートが整う | 左の手順 | 2026-09-20。§13.5。AM・AN は #28・#29 で使う。戻し忘れに注意（`agent:status` の「ノート作成」で確認） |
| AP | 保存済みセッションが 11 件以上あるとき、一覧は新しい順に 10 件だけ見え、下に「すべて表示（残り N 件）」が出る。押すと残りがふわっと出て、ボタンは「10 件だけ表示」になる。もう一度押すと畳まれ、ボタンが画面の外へ飛ばない。11 件目より古い講義を「やり直す」すると、畳んだ状態でもその行（処理中・「中止」）は見えたまま。パネルを開き直すと畳んだ状態に戻る。10 件以下ならボタンは出ない。ポップアップでも同じ（一覧の中でスクロール） | サイドパネルとポップアップで一覧を見る → 開閉 → 古い講義で「やり直す」 | 2026-09-20。§15.1。AM〜AO は #28〜#30 で使う |
| AQ | 中止まわり。①処理済みの講義で「やり直す」→ ノート作成中に「中止」→ 行は完了のまま（エラーも注意書きも増えない）、`notes.md` は前回のまま、`.lecscribe/pipeline.json` は `stage: done` で `result` が残り、`pipeline.previous.json` は消えている。続けて「やり直す」すると文字起こし（Whisper）は飛ばされる（server.log に「transcript を再利用」）。②新しく録った講義を送り、Server 行が「ノート作成」になってから「中止」→ 確認の文が「ノート作成を中止します…録音も残ります」になり、押すと数秒で完了して、行に「ノート作成を中止しました（文字起こしのままです）。「やり直す」で整えられます」が出る。録音も `~/LecScribe` のフォルダも残り、「やり直す」で整う。③新しく録った講義を、文字起こし中（ノート作成より前）に「中止」→ 今までどおり、確認のあとフォルダと録音が消える | 左の手順。①②は Codex が遅いと試しやすい（`LEC_SCRIBE_CODEX` を `sleep 60` するスクリプトに向けて `agent:install`。終わったら戻す） | 2026-09-20。§11.3。api 4 |
| AR | 注意書きが、サーバーの今の状態に合わせて直る。Codex が失敗する状態で 1 本「やり直す」→ ノート作成中に Chrome を終了する（完了を拡張が見届けない）→ サーバーが処理を終えるのを待って Chrome を開き直す → 一覧を開くと、その行に「ノートを整えられませんでした…」が出る。Codex を元に戻して「やり直す」→ また途中で Chrome を終了 → 終わってから開き直すと、注意書きが消えている | 左の手順（`LEC_SCRIBE_CODEX` で一時的に失敗させる。AO と同じ） | 2026-09-20。§11.3。スモークテストは OPFS の印を直接書き換えて同じことを見ている |
| AS | アイコンでサイドパネルを直接開く（ポップアップは出ない）。①動画ページでアイコン → 右にパネルが開き、Video 行に動画が出る → Start で録音が始まる。②Stop → 同じサイトの別のページ（次の章）へ移動 → パネルは開いたまま、Video 行が新しいページの動画に変わる（動画のないページを挟むと「動画が見つかりません」になり、動画のあるページで消える）→ Start できる。③何もしていないときに別のサイトへ移動 → パネルが閉じる。動画ページに戻ってアイコン → Start できる。④文字起こし中に別のサイトへ移動 → パネルは閉じない。処理が終わったあとの移動で閉じる。⑤動画のないタブでアイコン → パネルが開き、一覧から「やり直す」ができる。⑥パネルはアイコンを押したタブにだけ出る。⑦ウィンドウを 2 つ開き、片方でパネルを開いて別のウィンドウを前面にしても、パネルの Video 行と Start はパネルのいるウィンドウのタブを指す（別のウィンドウのページへ移動しても、こちらの Video 行は変わらない）。なお録音は同時 1 本なので、どちらのウィンドウで始めてもどのパネルも「録音中」になり、Start は隠れて Stop だけが出る（仕様）。⑧`http://127.0.0.1:8787/player.html` でパネルを開き、何もしないまま別のサイトへ移動 → パネルが閉じる（127.0.0.1 は URL が読めるので、以前は閉じなかった） | 左の手順 | 2026-09-21。D-12。①②④は実験用のブランチで確認済み。レビュー修正後のビルドで①③⑦⑧も確認済み（2026-09-21、MacBook Pro M3 Max） |
| AT | 動画のないページで、広告や計測用の見えない iframe（幅と高さが 0 の Criteo の同期用 iframe など）しか無いときは、「このページには動画が見つかりません」だけが出て、別ドメインの埋め込みの警告は出ない。YouTube などの見えるプレイヤーを埋め込んだページでは、「このページには別ドメインの埋め込みがあります。動画がその中にある場合、スライド画像は取得できません（音声は録音できます）。」も出る | 大学のサイトの動画のないページ → アイコン。YouTube の埋め込みがあるページ → アイコン | 2026-09-21。§8.1 |
| AU | 画像のまとめ 2 つ（§13.4b）。①GD I-2 の 2 章を「やり直す」と、Web ページをカード 1 行分ずつスクロールした 057・058 が 058 の 1 枚になる（059 は残る。053・055 は画面の半分以上が入れ替わるので残る）。GD I-3 の 5 章はピクトグラムの一覧を送った 028・030 が 030 に、11 章は Illustrator で描きながら大きく送った 023〜028 が 028 になる。②「はじめに」（自然を観る）を「やり直す」と、描く手元に字幕が出る 014〜016 が 016 の 1 枚に、005・006 が 006 になる。14 章（自然を観る）の「カタバミ」の別の株 026・027 は残る | 左の講義で「やり直す」→ `notes.md` の画像を見る | 2026-09-22。§13.4b。28 セッション 1,069 枚で変わるのは 15 枚（載せる画像 751 → 736）。文字起こしは再利用されるので Whisper は走らない |

## 過去セッションの再処理

サーバーを止めた状態で録音した分や、モデルを変えて処理し直したい分は、サイドパネルの一覧から「送信 / 再送」で再処理できる（サーバー側は同じフォルダに上書き）。

## 記録

| 日付 | Phase | 環境 | 結果 | メモ |
|---|---|---|---|---|
| 2026-09-08 | 1 | MacBook Pro M3 Max、内蔵スピーカー | OK | パススルー動作、二重再生なし、Stop 後も再生継続。手順 6（一時停止時のメーター）は popup がページ操作で閉じるため観察できず → UI をサイドパネルに変更 |
| 2026-09-08 | 2 | MacBook Pro M3 Max | OK | 録音・エクスポート・中断セッションの回収・破棄を確認。「破棄」で Downloads のファイルが残る点は仕様（ダイアログに明記） |
| 2026-09-08 | 3 | MacBook Pro M3 Max | OK | 実サイトで video.js の `<video>` を検出、状態が追従。再開直後に「タブが非表示です」が約 1 秒出る誤判定 → 再生開始・シーク時にフレーム計測をリセットして修正。別タブ表示中も警告が出ず録音・追跡が続いた（Chrome がキャプチャ中のタブを表示扱いにするため。手順 5 の期待値を変更） |
| 2026-09-08 | 4 | MacBook Pro M3 Max | OK | 開始時 1 枚 + 手動保存、別タブ表示中の保存、エクスポートを確認。画像は動画部分のみ。解像度は通常表示で 960×540、全画面で 1280×720（Brightcove の ABR がレンディションを切り替えるため。SPEC §8.3） |
| 2026-09-08 | 5 | MacBook Pro M3 Max | OK | 実際の動画でスライド切り替えに合わせて自動保存された（閾値 2% / 1.5%） |
| 2026-09-08 | 6 | MacBook Pro M3 Max | OK | 一時停止・シーク・1.5x を含む視聴で timeline.json が正しく記録された。tick の予測誤差 40〜65 ms。Brightcove が同じ位置で `seeked` を数回発火して重複していたため、直前と同じイベントは記録しないよう改善 |
| 2026-09-08 | 7 | MacBook Pro M3 Max | OK | 実際の動画 131 秒の録音で送信 → 文字起こし → `~/LecScribe/…/transcript.srt` まで通った（35 区間、動画時刻に変換済み）。初回は `large-v3` のダウンロード（1.25 GB、約 2 分）と CoreML の初回コンパイルで文字起こし段階が 441 秒。キャッシュ済みの 2 回目は 41.9 秒（モデル読み込み込みで約 3 倍速）。16 分の動画では Done まで約 5 分（約 3.2 倍速、90 分なら 25〜30 分の見込み）。同じ音声（966 秒）を `large-v3_turbo` で処理すると初回 846 秒（ダウンロード・コンパイル込み）、2 回目 248 秒（約 3.9 倍速）。区間数 297 で同じ、冒頭の本文も同一で精度差は見えず。速度差が 2 割程度なので既定は `large-v3` のまま（`--model large-v3_turbo` で切替可）。エンコーダ・デコーダを GPU（`--*-compute-units cpuAndGPU`）にすると 2 回目 315 秒で Neural Engine より遅く、区間の切り方も変わった（177 区間）ため不採用 |
| 2026-09-08 | 8 | MacBook Pro M3 Max | OK | 16 分の動画で `lecture.md` を確認。「フォルダを開く」もサーバー経由で動作 |
| 2026-09-09 | 9 | MacBook Pro M3 Max、Codex CLI 0.153（ChatGPT Go） | OK | 16 分の動画（17 節、約 6,000 字）を `codex exec` 2 回・100 秒で処理。フィラーが消え段落分けと要点が付いた `notes.md` ができた。内容の改変は見当たらず |
| 2026-09-09 | 残 A, E, F | MacBook Pro M3 Max | OK | 文字起こし中の「破棄」で処理が止まり、`~/LecScribe/` のフォルダと拡張内の録音が消えた。ポップアップの Start 1 回で録音が始まりサイドパネルが開く。録音中もセッション一覧が表示される |
| 2026-09-09 | 残 H | MacBook Pro M3 Max | OK | plist に `LEC_SCRIBE_LLM=codex` が入っている（起動ログに `llm` 行を出すようにした） |
| 2026-09-09 | 残 B | MacBook Pro M3 Max | OK | 文字起こし中に破棄したあと `ps` に whisperkit-cli は残らなかった。`codex` として見えたのは ChatGPT アプリ内蔵の Codex（前日から起動）で、サーバーが呼ぶ `codex exec` ではない。ノート作成中に破棄した場合の `codex exec` の後始末は未確認 |
| 2026-09-09 | 残 N | MacBook Pro M3 Max | OK | 「このMacと接続」→ Mac のダイアログで「許可」→ サーバーのログに `paired: <拡張 ID> (LecScribe)`、`trusted.json` に記録され、以後トークンの貼り付けなしで送信できた |
