{
  "name": "@lec-scribe/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "prepare:types": "wxt prepare",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"(lint: not configured yet)\""
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0",
    "wxt": "0.21.4"
  }
}
