/**
 * 実験: アイコンを押したら、ポップアップを挟まずにサイドパネルを直接開く（2026-09-21）。
 *
 * これまではアイコン → ポップアップ → Start → サイドパネル、だった（SPEC D-12）。録音の開始には「アイコンを押した
 * 時点のタブ」にだけ出る許可（activeTab）が要り、開きっぱなしのパネルからの Start は許可切れで失敗したため。
 * この実験では、アイコンのクリック（action.onClicked。activeTab が出る正規のきっかけ）でそのタブにパネルを開き、
 * 別のサイトへ移動して許可が切れたら、何もしていないときに限ってパネルを閉じる。次に使うときは必ずアイコンを
 * 押すことになるので、許可も新しくなる。
 *
 * Mac 実機で確かめること（tabCapture は headless では動かない）:
 *   1. アイコンでパネルを開き、すぐ Start できる
 *   2. 同じサイトの中で別のページへ移動してから Start できる
 *   3. 別のサイトへ移動するとパネルが閉じ、アイコンを押し直すと Start できる
 *
 * false にすると今までどおり（アイコン → ポップアップ）。
 */
export const PANEL_FIRST = true;
